import 'package:flutter/material.dart';

class AppColors {
  // 🔥 PRIMARY (Main Brand)
  static const primary = Color(0xFFFF8A00);
  static const secondary = Color(0xFFFF6A00);

  // 🌈 LOGIN GRADIENT (FIX FOR YOUR ERROR)
  static const backgroundTop = Color(0xFFFF8A00);
  static const backgroundBottom = Color(0xFFFF6A00);

  // 🌗 LIGHT MODE
  static const backgroundLight = Color(0xFFF5F5F5);
  static const cardLight = Colors.white;

  // 🌙 DARK MODE
  static const backgroundDark = Color(0xFF121212);
  static const cardDark = Color(0xFF1E1E1E);

  // 🧾 COMMON (USED IN UI DIRECTLY)
  static const card = Colors.white; // 🔥 important for login screen

  // 📝 TEXT
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF777777);

  // 🔘 BORDER
  static const border = Color(0xFFE0E0E0);

  // ⚠️ STATUS COLORS
  static const success = Colors.green;
  static const warning = Colors.orange;
  static const error = Colors.red;
}
